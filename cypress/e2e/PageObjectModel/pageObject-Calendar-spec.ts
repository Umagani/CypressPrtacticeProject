describe('POM Test for Shop App', () => {
    beforeEach(() => {
        cy.visit(Cypress.config('baseUrl') + Cypress.env('POMUrl'));
        //cy.visit('/');
    });

    it('Create a test for Shop App in POM Model', () => {

    });

});